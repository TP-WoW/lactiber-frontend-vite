import { memo } from 'react';
import WorkInProgress from '@/components/WorkInProgress';

const Reports = () => {
  return <WorkInProgress title="Reports" subtitle="Reporting features are being developed. Check back soon." />;
};

export default memo(Reports);